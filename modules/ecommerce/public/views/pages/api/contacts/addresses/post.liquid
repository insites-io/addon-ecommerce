---
slug: api/create-contact-address
format: json
method: post
searchable: false
---

{%- assign payload = context.params | parse_json -%}
{%- assign status = 400 -%}

{%- parse_json properties -%}
{
     "properties": {{ payload | default: '[]' }}
}
{%- endparse_json -%}

{% comment %} Use Insites V2 API {% endcomment %}
{%- graphql add_contact_address = 'modules/website/trigger_api_call', 
     name: 'modules/portal/add_contact_address', 
     data: properties 
     | dig: 'items','response','body' | parse_json -%}            

{% comment %} Set Status {% endcomment %}
{%- liquid
     if add_contact_address.id 
          assign status = 200
     endif
-%}

{{ add_contact_address  }}
{%- response_status status -%}