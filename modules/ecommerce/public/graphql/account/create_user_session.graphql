mutation create_user_session(
  $email: String!, 
  $password: String!
  ) {
  user_session_create(
    form_name: "modules/ecommerce/sign_in" # must match with `name` of your form
    email: $email
    password: $password
  ) {
    email
  }
}