mutation set_email_verification_token(
  $id: ID!
  $token: String!
) {
  user_update(
    id: $id
    user: {
      profiles: [
        {
          name: "modules/insites_core/token",
          values: {
            properties: [
              { name: "password_token", value: $token }
            ]
          }
        }
      ]
    }
    form_name: "modules/ecommerce/set_email_verification_token"
  ) {
    id
  }
}