query get_user_id($email:String!){
  items: users(
    page: 1,
    per_page: 1,
    filter: {
      deleted_at: { exists:false }
      email: { value: $email }
    }
  ){
    results{
      id
    }
  }
} 